{
  "name": "destiny",
  "description": "A reactive UI library for JavaScript and TypeScript.",
  "version": "0.4.0",
  "main": "./dist/examples/main.js",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "deno run --allow-read=. --unstable console/create_dist.ts",
    "watch": "deno run --allow-read=. --unstable console/create_dist.ts --watch",
    "fmt": "deno fmt --check --ignore=dist",
    "lint": "deno lint --unstable --ignore=dist"
  },
  "keywords": [],
  "repository": "https://github.com/0kku/destiny",
  "author": "https://github.com/0kku/",
  "license": "OSL-3.0"
}
